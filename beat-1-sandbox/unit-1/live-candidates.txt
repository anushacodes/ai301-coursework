All three issues verified against live source and tests. Now I have everything needed to grade.

## Repo-level facts (shared across all three)

- **Archived:** false. Last push 2026-09-16. Last 5 commits all by human `Aburke225` (Sep 16 and Aug 24, 2026) — chore/docs work, not bot noise.
- **Releases:** none found (empty list) — acceptable per rubric since commit evidence meets the condition.
- **Contribution policy:** `docs/CONTRIBUTING.md` exists (fetched verbatim, 6.6KB) — covers branching, commits, CI, xfail-marker removal for seeded bugs. **No AI-related restriction anywhere** in CONTRIBUTING.md, PR template, or repo docs. No `AI_POLICY.md`/`AGENTS.md` found.
- House rule (Path Review): classmate claim comments never block an issue or fail "Work available."

## Per-issue grading

### Issue #69 — Output parser crashes on top-level JSON array fallback

| Check | Grade | Evidence |
|---|---|---|
| Active maintenance | pass | Human commit by Aburke225 on 2026-09-16 (within 90 days of 2026-09-20). |
| Project still maintained | pass | Not archived; human commit 2026-09-16 on default branch. |
| Bounded scope | pass | Single function fix in `_parse_json_output`/fallback path in `rag/generator/output_parser.py`; one clear outcome. |
| Contribution still needed | pass | Issue open; verified live in source: `_parse_json_output(data)` still calls `data.items()` unconditionally — bug still present; matching test `test_json_array_fallback` still has `@pytest.mark.xfail(..., reason="issue #69 (manifest H-02)...")`. |
| Work available | pass | Assignees: none. Comments: none. No linked PRs. |
| AI workflow allowed | pass | `docs/CONTRIBUTING.md` fetched in full — no AI restriction stated; silence passes per rubric. |
| Clear verification (preferred) | pass | `test_json_array_fallback` in `tests/unit/test_output_parser.py`, xfail strict=True, becomes the fix's test; removing the marker is the completion criterion. |
| Clear starting point (preferred) | pass | Issue names exact file/function (`output_parser.py`, the `.items()` call site) and it matches the live source verbatim. |

**Verdict: accept**

### Issue #68 — Keyword search raises `ZeroDivisionError` when index is empty

| Check | Grade | Evidence |
|---|---|---|
| Active maintenance | pass | Same repo-level commit evidence (Aburke225, 2026-09-16). |
| Project still maintained | pass | Same as above. |
| Bounded scope | pass | Single fix in `KeywordSearcher.index()` in `rag/retriever/keyword_search.py`, mirroring the empty-list guard already in `search()`. |
| Contribution still needed | pass | Live source confirms `index()` still calls `BM25Okapi(tokenized_corpus)` with no empty-check, unlike `search()` which already guards. Test still `xfail` with `reason="issue #68 (manifest H-01)..."`. |
| Work available | pass | Assignees: none. One comment from `acordero4852` (association: NONE, a fellow student) on 2026-09-19: "I'd like to take this one on... I'll report back." Under the **Path Review house rule**, a classmate's claim comment does not block the issue — claim anyway. No maintainer reservation, no linked/open PR. |
| AI workflow allowed | pass | Same CONTRIBUTING.md evidence — no restriction. |
| Clear verification (preferred) | pass | `test_empty_index` xfail strict=True tied to manifest H-01; concrete test to un-mark. |
| Clear starting point (preferred) | pass | Issue names exact file/method and the working, already-correct pattern in `search()` to mirror. |

**Verdict: accept**

### Issue #57 — Tech detector doesn't exclude `node_modules/`/`build/`

| Check | Grade | Evidence |
|---|---|---|
| Active maintenance | pass | Same repo-level commit evidence. |
| Project still maintained | pass | Same as above. |
| Bounded scope | pass | Single fix inside `TechDetector._should_skip_file` in `agent/tools/tech_detector.py`. |
| Contribution still needed | pass | Live source shows `_should_skip_file` checks `"/node_modules/" in filepath`, which fails to match a repo-root-relative path like `"node_modules/package1/index.js"` (no leading slash) — bug reproduced by reading the code. Both `test_node_modules_excluded` and `test_build_directory_excluded` in `tests/unit/test_tech_detector.py` are still `xfail(strict=True, reason="issue #57: tech detector counts vendored/build-output files")`. |
| Work available | pass | Assignees: none. No comments on the issue. No linked PRs. |
| AI workflow allowed | pass | Same CONTRIBUTING.md evidence — no restriction. |
| Clear verification (preferred) | pass | Two named, already-written xfail tests give concrete pass/fail criteria. |
| Clear starting point (preferred) | pass | Issue and tests both point to the same file/method; the root cause (missing-leading-slash substring match) is directly visible in the fetched source. |

**Verdict: accept**

## Ranked read-out (fit profile: backend Python/TypeScript, AI-tooling focus — model output handling, retrieval, or agent tools; 4–6 hrs budget)

1. **#68 — Keyword search `ZeroDivisionError`** (top fit). Backend Python fix inside a retrieval component (`rag/retriever/keyword_search.py`, BM25 sparse search) — squarely "handling... retrieval" from the fit profile. Smallest, most mechanical fix (mirror an existing guard from `search()` into `index()`), one pre-written xfail test, ~2–4 hr estimate fits the budget with margin.
2. **#69 — Output parser crash on JSON array fallback**. Also backend Python, directly "handling model output" (parsing LLM responses in `rag/generator/output_parser.py`) — a very close second for fit. Slightly more judgment needed (deciding how the array fallback should behave) than #68's guard-mirroring, but still one bounded function fix with a named test.
3. **#57 — Tech detector path-filter bug**. Backend Python and touches an agent tool (`agent/tools/tech_detector.py`), matching "an agent tool" in the fit profile, but it's general repo-scanning logic rather than model-output/retrieval work, so it fits the stated preference less tightly than the other two. Root cause (substring match missing a leading slash) is subtle enough to require careful reading, though still well within a first-issue's scope.

All three are backend Python bugs with matching, currently-failing `xfail` tests and no assignee or blocking claim — any of the three would be a reasonable first PR, but #68 best matches the stated interest in retrieval work with the lowest complexity.

```json
[
  {
    "item": "https://github.com/codepath/pathreview-ai301-fa26-s3/issues/68",
    "checks": [
      {"name": "Active maintenance", "grade": "pass", "evidence": "Human commit by Aburke225 on 2026-09-16, within 90 days of 2026-09-20."},
      {"name": "Project still maintained", "grade": "pass", "evidence": "Repo not archived; human default-branch commit on 2026-09-16."},
      {"name": "Bounded scope", "grade": "pass", "evidence": "Single fix in KeywordSearcher.index() in rag/retriever/keyword_search.py to mirror the empty-guard already present in search()."},
      {"name": "Contribution still needed", "grade": "pass", "evidence": "Live source shows index() calls BM25Okapi(tokenized_corpus) with no empty check; test_empty_index still marked xfail(strict=True, reason='issue #68 (manifest H-01)...')."},
      {"name": "Work available", "grade": "pass", "evidence": "Assignees: none; one classmate claim comment (acordero4852, association NONE, 2026-09-19) does not block under the Path Review house rule; no linked/open PR."},
      {"name": "AI workflow allowed", "grade": "pass", "evidence": "docs/CONTRIBUTING.md fetched in full; states CI/branching/xfail rules but no AI-use restriction."},
      {"name": "Clear verification", "grade": "pass", "evidence": "test_empty_index in tests/unit/test_keyword_search.py, xfail strict=True tied to manifest H-01."},
      {"name": "Clear starting point", "grade": "pass", "evidence": "Issue names exact file/method (rag/retriever/keyword_search.py KeywordSearcher.index()) and the working pattern in search() to mirror, confirmed against live source."}
    ],
    "verdict": "accept"
  },
  {
    "item": "https://github.com/codepath/pathreview-ai301-fa26-s3/issues/69",
    "checks": [
      {"name": "Active maintenance", "grade": "pass", "evidence": "Human commit by Aburke225 on 2026-09-16, within 90 days of 2026-09-20."},
      {"name": "Project still maintained", "grade": "pass", "evidence": "Repo not archived; human default-branch commit on 2026-09-16."},
      {"name": "Bounded scope", "grade": "pass", "evidence": "Single fix to the fallback path in _parse_json_output in rag/generator/output_parser.py."},
      {"name": "Contribution still needed", "grade": "pass", "evidence": "Live source shows _parse_json_output(data) unconditionally calls data.items(); test_json_array_fallback still marked xfail(strict=True, reason='issue #69 (manifest H-02)...')."},
      {"name": "Work available", "grade": "pass", "evidence": "Assignees: none; no comments; no linked PRs."},
      {"name": "AI workflow allowed", "grade": "pass", "evidence": "docs/CONTRIBUTING.md fetched in full; no AI-use restriction stated."},
      {"name": "Clear verification", "grade": "pass", "evidence": "test_json_array_fallback in tests/unit/test_output_parser.py, xfail strict=True tied to manifest H-02."},
      {"name": "Clear starting point", "grade": "pass", "evidence": "Issue names exact file/call site (output_parser.py, the .items() call), confirmed verbatim against live source."}
    ],
    "verdict": "accept"
  },
  {
    "item": "https://github.com/codepath/pathreview-ai301-fa26-s3/issues/57",
    "checks": [
      {"name": "Active maintenance", "grade": "pass", "evidence": "Human commit by Aburke225 on 2026-09-16, within 90 days of 2026-09-20."},
      {"name": "Project still maintained", "grade": "pass", "evidence": "Repo not archived; human default-branch commit on 2026-09-16."},
      {"name": "Bounded scope", "grade": "pass", "evidence": "Single fix inside TechDetector._should_skip_file in agent/tools/tech_detector.py."},
      {"name": "Contribution still needed", "grade": "pass", "evidence": "Live source shows _should_skip_file checks '/node_modules/' in filepath, which misses root-relative paths like 'node_modules/pkg/index.js'; test_node_modules_excluded and test_build_directory_excluded still marked xfail(strict=True, reason='issue #57...')."},
      {"name": "Work available", "grade": "pass", "evidence": "Assignees: none; no comments; no linked PRs."},
      {"name": "AI workflow allowed", "grade": "pass", "evidence": "docs/CONTRIBUTING.md fetched in full; no AI-use restriction stated."},
      {"name": "Clear verification", "grade": "pass", "evidence": "Two named xfail tests (test_node_modules_excluded, test_build_directory_excluded) in tests/unit/test_tech_detector.py give concrete pass/fail criteria."},
      {"name": "Clear starting point", "grade": "pass", "evidence": "Issue and tests both point to the same file/method; root cause (missing-leading-slash substring match) visible directly in fetched source."}
    ],
    "verdict": "accept"
  }
]
```
